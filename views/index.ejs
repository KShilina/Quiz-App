<!DOCTYPE html>
<html lang="en">
  <title>Home Page</title>
  <body class="main-body">
    <%- include('./partials/_headers') %>



  <div class="main-box">
    <p class="main-paragraph">This application allows you to create and take your own, and others coding quizzes. Expand your knowledge and learn new topics by pressing one of the quizzes below. Alternatively, allow others to learn and understand by creating your own quiz.</p>
  </div>
  <div class="three-boxes">

    <% for (let quiz of quizzes) { %>
      <form action="/users/users_quizzes/quiz/<%= quiz.id %>" method="GET" class="my-quizzes">
        <button class="my-quiz-btn" type="submit">
          <p class="home"> <%= quiz.title %></p>
        </button>
      </form>
      <% } %>


    <!-- <div class="small-box" onclick="location.href='/users/users_quizzes/quiz/<%= quizzes[quizzes.length - 1] %>'>
      <p> <%= quizzes[quizzes.length - 1].title %></p>
    </div>

    <div class="small-box">
      <p> <%= quizzes[quizzes.length - 2].title %></p>

    </div>
    <div class="small-box">
      <p> <%= quizzes[quizzes .length - 3].title %></p>

    </div> -->
  </div>
  </body>
    </div>
  </body>
</html>
